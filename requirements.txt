# iPhone access (detection, AFC media read/delete) — pure Python, works on Windows.
# Pinned below 8.0: pymobiledevice3 8.x+ rewrote the API to async; this app uses
# the synchronous API (7.x is the last sync line).
pymobiledevice3>=7.0.0,<8

# Windows-only: pymobiledevice3 7.x imports win32security at runtime but does
# not declare pywin32 as a dependency, so we install it explicitly.
pywin32>=306; sys_platform == "win32"

# GUI
PySide6>=6.6.0

# Image handling: HEIC decoding + perceptual hashing
Pillow>=10.0.0
pillow-heif>=0.15.0
ImageHash>=4.3.1

# Optional offline OCR (text detection for "Probably Delete"). The Tesseract
# engine itself is bundled into the Windows build separately; this is just the
# Python wrapper. Free / Apache-2.0.
pytesseract>=0.3.10
